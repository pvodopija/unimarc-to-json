# unimarc-to-json
UNIMARC to JSON converter written in Haskell. For example, it takes this [input](/src/unimarc.txt) file and produces this JSON [output](output.json). <br/> To build and run simply do:
```
  stack build
  stack exec unimarc-haskell input.txt
```
